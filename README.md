# AI-Model-Performance-Simulator

This project simulates how machine learning models behave under changing real-world conditions. It introduces controlled data drift, noise, imbalance, and distribution shifts into datasets. The system evaluates performance degradation across multiple metrics over time. It helps identify model robustness limits and failure patterns before deployment.
